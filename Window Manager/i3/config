# <<--------- General Settings --------->>

font pango:Roboto Regular 11
floating_modifier $mod
set         $term --no-startup-id st
set         $mod Mod4
set         $screencast bash ~/.config/scripts/cast
exec        --no-startup-id dex --autostart --environment i3
exec        --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec        xrdb -merge ~/.Xresources
exec        --no-startup-id nm-applet
set         $refresh_i3status killall -SIGUSR1 i3status
bindsym     XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym     XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym     XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym     XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# <<--------- Focus Control --------->>

bindsym $mod+Shift+space    floating toggle
bindsym $mod+space          focus mode_toggle
bindsym $mod+a              focus parent
bindsym $mod+Left           focus left
bindsym $mod+Down           focus down
bindsym $mod+Up             focus up
bindsym $mod+Right          focus right
bindsym $mod+Shift+Left     move left
bindsym $mod+Shift+Down     move down
bindsym $mod+Shift+Up       move up
bindsym $mod+Shift+Right    move right

# <<--------- Workspaces --------->>

set $ws1 "1: "
set $ws2 "2: "
set $ws3 "3: "
set $ws4 "4: "
set $ws5 "5: " 
set $ws6 "6: "
set $ws7 "7: "
set $ws8 "8: "
set $ws9 "9: "
set $ws10 "10: "

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# <<--------- Workspace Navigation --------->>

bindsym $mod+Home	workspace $ws1
bindsym $mod+End	workspace $ws10
bindsym $mod+Next	workspace prev
bindsym $mod+Prior	workspace next
bindsym $mod+Tab	workspace back_and_forth

# <<--------- Moving Containers --------->>

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# <<--------- i3 Status Bar --------->>

# <<---- Colors ---->>

#bar {
#     status_command i3blocks -c /home/core/.config/i3blocks/i3blocks.conf
#     status_command i3status -c /home/core/.config/i3status/i3status.conf
#     i3bar_command i3bar --transparency
#     position top
#}


# <<--------- Assign Programs To Workspaces -------->>

for_window [class="Brave-browser"]		move to workspace $ws1
for_window [class="Rhythmbox"]          move to workspace $ws8
for_window [class="TelegramDesktop"]	position center
for_window [class="TelegramDesktop"]	floating enable
for_window [title="OBS*"]				move to workspace $ws9
for_window [title="dropcalc"]			floating enable
for_window [title="dropcalc"]			resize set 800 300
for_window [title="dropcalc"]			move position center
for_window [title="droptodo"]			floating enable
for_window [title="dropdict"]			floating enable
for_window [title="dropurl"]			floating enable
for_window [title="dropbiblo"]			floating enable
for_window [title="dropgrammar"]		floating enable
for_window [title="dropquote"]			floating enable
for_window [instance="dropterm"]		floating enable
for_window [instance="dropterm"]		move scratchpad
for_window [instance="dropvpn"]         floating enable
for_window [instance="dropvpn"]         move scratchpad


# <<--------- Startup & Scripts --------->>

exec                --no-startup-id conky
exec_always         --no-startup-id $HOME/.config/polybar/launch.sh
exec                nvidia-settings --load-config-only
exec                xset r rate 300 70
# exec_always         --no-startup-id numlockx on
exec_always         /usr/bin/dunst
exec_always         /home/core/.config/scripts/event
exec_always         /home/core/.config/scripts/dator
exec_always 		picom --experimental-backends -b
exec                --no-startup-id $term -n dropterm
exec                --no-startup-id $term -n dropvpn 
exec_always         "setxkbmap -model pc104 -layout us,ir -variant ,, -option grp:ctrl_shift_toggle"
exec_always         /home/core/.config/scripts/layout && $refresh_i3status
exec_always         feh --bg-fill ~/Media/Pictures/Walls/4.png
# exec_always         wal -R

# <<--------- Bindings --------->>

#bindsym $mod+mod2+KP_0
#bindsym $mod+mod2+KP_1 
#bindsym $mod+mod2+KP_2 
#bindsym $mod+mod2+KP_3 
#bindsym $mod+mod2+KP_4 
#bindsym $mod+mod2+KP_5 
#bindsym $mod+mod2+KP_6 
#bindsym $mod+mod2+KP_7 
#bindsym $mod+mod2+KP_8 
#bindsym $mod+mod2+KP_9 
#bindsym $mod+mod2+KP_Add
#bindsym $mod+mod2+KP_Subtract
#bindsym $mod+mod2+KP_Multiply
#bindsym $mod+mod2+KP_Divide
#bindsym $mod+mod2+KP_Enter
#bindsym $mod+mod2+KP_Separator

# bindsym $mod+s		    layout stacking
# bindsym $mod+d exec --no-startup-id dmenu_run
# bindsym $mod+t		    layout tabbed

bindsym Print               exec flameshot gui
bindsym $mod+w			    exec --no-startup-id brave
bindsym $mod+shift+w	    exec --no-startup-id qutebrowser
bindsym $mod+l 			    exec --no-startup-id thunar
bindsym $mod+shift+l 	    exec --no-startup-id nautilus
bindsym $mod+o			    exec --no-startup-id obsidian
bindsym $mod+k			    exec --no-startup-id anki
bindsym $mod+shift+Return   exec --no-startup-id cool-retro-term
bindsym $mod+z			    exec --no-startup-id zotero
bindsym $mod+g 			    exec telegram-desktop -g 78x24+50+20
bindsym $mod+shift+s        exec $screencast
bindsym $mod+F12		    exec i3-input -F 'exec goldendict %s' -P 'Word: '
bindsym $mod+p 			    exec sxiv -t /home/core/Media/Pictures/Wallpapers/*
bindsym $mod+s			    scratchpad show; [instance="dropterm"] move position center
bindsym $mod+shift+v		scratchpad show; [instance="dropvpn"] move position center
bindsym $mod+Return 	    exec terminator
bindsym $mod+u 			    exec $term -t "dropurl" -e nvim ~/.config/scripts/url.txt 
bindsym $mod+b			    exec $term -t "dropbiblo" -e nvim ~/Documents/Notes/Templates/biblo.bib
bindsym $mod+c			    exec $term -t "dropcalc" -f mono:pixelsize=24 -e R --quiet
bindsym $mod+t			    exec $term -t "droptodo" -e nvim ~/Documents/Notes/todo/Todo.wiki
bindsym $mod+shift+q 	    exec $term -t "dropquote" -e nvim ~/Documents/Obsidian/Broadest\ Education/1\ -\ Inbox/quotivation.md
bindsym $mod+shift+g 	    exec $term -t "dropgrammar" -e nvim ~/Documents/Notes/Grammar/Grammar.tex
bindsym $mod+F4			    exec $term -t "dropdict" -e nvim ~/Documents/Obsidian/Broadest\ Education/9\ -\ Anki/Dictionary.md
bindsym $mod+v			    exec $term -e /home/core/.config/scripts/lfub
bindsym $mod+n			    exec $term -e newsboat
# bindsym $mod+m			    exec $term -e ncmpcpp
bindsym $mod+m			    exec --no-startup-id rhythmbox
bindsym $mod+semicolon	    layout toggle split
bindsym $mod+backslash	    split h
bindsym $mod+slash	        split v
bindsym $mod+Shift+r        restart
bindsym $mod+f		        fullscreen toggle
bindsym $mod+q              kill
bindsym $mod+d              exec rofi -show run -show-icons
bindsym $mod+shift+x	    exec /home/core/.config/scripts/lock

# <<--------- Window Resize --------->>

bindsym $mod+r mode "resize"
bindsym $mod+Shift+Escape exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
mode "resize" {
bindsym j resize shrink width 10 px or 10 ppt
bindsym k resize grow height 10 px or 10 ppt
bindsym l resize shrink height 10 px or 10 ppt
bindsym semicolon resize grow width 10 px or 10 ppt
bindsym Left resize shrink width 10 px or 10 ppt
bindsym Down resize grow height 10 px or 10 ppt
bindsym Up resize shrink height 10 px or 10 ppt
bindsym Right resize grow width 10 px or 10 ppt
bindsym Return mode "default"
bindsym Escape mode "default"
bindsym $mod+r mode "default"
}

# <<--------- Gaps Control --------->>

gaps inner 15
gaps outer 15
for_window [class="^.*"]    border pixel 2
bindsym $mod+Shift+minus	gaps inner current plus 5
bindsym $mod+Shift+plus		gaps inner current minus 5
bindsym $mod+Shift+d		gaps inner current set 0; gaps outer current set 0

# <<--------- System Power Control --------->>

set $mode_system System (e) logout, (r) reboot, (s) sleep, (Shift+s) shutdown
mode "$mode_system" {
	bindsym e exec --no-startup-id i3-msg exit, mode "default" 
	bindsym r exec --no-startup-id systemctl reboot, mode "default"
	bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"
	bindsym s exec --no-startup-id systemctl suspend, mode "default"
	bindsym Return mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+F1 mode "$mode_system"
